import {SOURCES} from './sources.js';
import {db,insertEvent} from './db.js';
export async function checkSourceHealth(){const results=[];for(const source of SOURCES){const started=Date.now(),checkedAt=new Date().toISOString();try{const r=await fetch(source.url,{method:'GET',redirect:'follow',headers:{'user-agent':'ProcessPilot/1.0 procurement-source-health'}});const ok=r.ok,result={source_key:source.code,name:source.name,jurisdiction:source.jurisdiction,reachable:ok,http_status:r.status,latency_ms:Date.now()-started,checked_at:checkedAt,collector_status:'discovery_pending'};results.push(result);const patch={last_checked_at:checkedAt,last_error:ok?null:`HTTP ${r.status}`,config:{health:{http_status:r.status,latency_ms:result.latency_ms,collector_status:'discovery_pending'}}};if(ok)patch.last_success_at=checkedAt;await db(`source_registry?code=eq.${encodeURIComponent(source.code)}`,{method:'PATCH',body:JSON.stringify(patch)}).catch(()=>{});}catch(e){const result={source_key:source.code,name:source.name,jurisdiction:source.jurisdiction,reachable:false,error:e.message,latency_ms:Date.now()-started,checked_at:checkedAt,collector_status:'discovery_pending'};results.push(result);await db(`source_registry?code=eq.${encodeURIComponent(source.code)}`,{method:'PATCH',body:JSON.stringify({last_checked_at:checkedAt,last_error:e.message,config:{health:{error:e.message,collector_status:'discovery_pending'}}})}).catch(()=>{});}}await insertEvent({agent_id:'opportunity-scout',event_type:'source_health_checked',message:`Checked ${results.length} priority procurement portals; reachability is not treated as successful opportunity collection.`,severity:results.every(x=>x.reachable)?'info':'warning',verified:true,source:'source-health',metadata:{sources:results}});return results;}

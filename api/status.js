import fs from 'node:fs';import path from 'node:path';import {db,listApprovals,listEvents,listWork,listOpportunities} from '../lib/db.js';function read(name){return JSON.parse(fs.readFileSync(path.join(process.cwd(),'data',name),'utf8'))}
export default async function handler(req,res){const agents=read('agents.json').agents;let states={};let work=[],approvals=[],events=[],opportunities=[];try{const rows=await db('integration_status?select=integration_key,status');states=Object.fromEntries((rows||[]).map(x=>[x.integration_key,x.status]));[work,approvals,events,opportunities]=await Promise.all([listWork(),listApprovals(),listEvents(30),listOpportunities()]);}catch(e){}
const integrations={openai:Boolean(process.env.OPENAI_API_KEY||process.env.AI_GATEWAY_API_KEY||process.env.VERCEL_OIDC_TOKEN)||states.ai==='ready',database:Boolean(process.env.DATABASE_URL||(process.env.SUPABASE_URL&&process.env.SUPABASE_SERVICE_ROLE_KEY)),gmail:states.gmail==='ready',procurementFeeds:states.procurement_feeds==='ready'};const researchReady=integrations.openai&&integrations.database&&integrations.procurementFeeds,draftingReady=integrations.openai&&integrations.database,communicationReady=draftingReady&&integrations.gmail;const departments={'Opportunity Intelligence':researchReady?'ready':'waiting','Procurement Operations':researchReady?'ready':'waiting','Vendor Research':researchReady?'ready':'waiting','Proposal Studio':draftingReady?'ready':'waiting','Compliance & Risk':draftingReady?'ready':'waiting','Pricing & Finance':draftingReady?'ready':'waiting','Outreach':communicationReady?'ready':'waiting','Contracts & Delivery':draftingReady?'ready':'waiting','Executive Operations':draftingReady?'ready':'waiting'};const readyAgents=agents.filter(a=>departments[a.department]==='ready').length;const count=s=>opportunities.filter(o=>o.status===s).length;res.setHeader('Cache-Control','no-store');res.status(200).json({mode:'live-readiness',generatedAt:new Date().toISOString(),integrations,departments,readyAgents,blockedAgents:agents.length-readyAgents,queueDepth:work.filter(w=>['queued','running','waiting_input','waiting_approval'].includes(w.status)).length,approvals:approvals.filter(a=>a.status==='pending'),events,progress:{found:opportunities.length,qualified:opportunities.filter(o=>Number(o.fit_score)>=60).length,proposals:count('proposal'),submitted:count('submitted'),awards:count('awarded')},notice:'Only persisted and verified work is reported.'});}
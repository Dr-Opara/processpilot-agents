import {getGmailProfile,gmailConfigured} from '../../lib/gmail.js';
export default async function handler(req,res){if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});if(!gmailConfigured())return res.status(503).json({ready:false});try{await getGmailProfile();return res.status(200).json({ready:true,allowedSenders:['eo@processpilottech.com','contracts@processpilottech.com'],note:'Underlying Google mailbox identity is intentionally not exposed.'});}catch(e){return res.status(503).json({ready:false,error:'Gmail runtime verification failed'});}}

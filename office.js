let registry={agents:[]},runtime=null;const $=s=>document.querySelector(s);const COLORS=['#22d3ee','#4f9cff','#9b70ff','#18e69b','#f2b94b','#e96cd8','#ff6a55'];const SPOTS=[[20,30],[80,30],[15,55],[85,53],[25,74],[75,74],[48,57],[56,54],[41,50],[63,47],[35,44],[70,41],[28,39],[58,38],[45,36],[68,34],[38,32],[61,31],[31,28],[73,27],[42,25],[66,24],[34,61],[62,62],[18,66],[83,66],[39,69],[61,69],[50,72],[89,72]];async function json(url){const r=await fetch(url,{cache:'no-store'});if(!r.ok)throw new Error(`${url}: ${r.status}`);return r.json()}async function load(){try{const [a,j]=await Promise.all([json('/data/agents.json'),json('/data/jurisdictions.json')]);registry=a;runtime=await json('/api/status').catch(()=>null);renderFloor();renderStatus(j);wireImage()}catch(e){console.error(e);$('#systemState').textContent='Configuration error'}}function state(id){return runtime?.agentStates?.[id]||'live_idle'}function stateLabel(id){return({working:'Working now',awaiting_approval:'Awaiting approval',live_idle:'Live · waiting',blocked:'Blocked'})[state(id)]||'Live · waiting'}function renderFloor(){const root=$('#agentFloor');root.innerHTML='';registry.agents.forEach((a,i)=>{const [x,y]=SPOTS[i%SPOTS.length],s=state(a.id),node=document.createElement('button');node.className=`agent-node ${s}`;node.style.left=x+'%';node.style.top=y+'%';node.style.setProperty('--agentColor',COLORS[i%COLORS.length]);node.innerHTML=`<div class="agent-tag"><strong>${escapeHtml(a.name)}</strong><small>${stateLabel(a.id)}</small></div>`;node.title=`${a.name} — ${stateLabel(a.id)}`;node.onclick=()=>showAgent(a.id);root.append(node)})}function renderStatus(j){const n=registry.agents.length,ready=runtime?.readyAgents??0,blocked=runtime?.blockedAgents??n,decisions=runtime?.approvals??[],events=runtime?.events??[],p=runtime?.progress||{};$('#agentCount').textContent=n;$('#readyCount').textContent=`${ready} / ${n} runtime-ready`;$('#systemState').textContent=blocked?'Partially connected':'All Systems Operational';$('#approvalMetric').textContent=decisions.length;$('#opportunityMetric').textContent=p.found||0;$('#proposalMetric').textContent=p.proposals||0;$('#outreachMetric').textContent=p.outreach||0;$('#completedTotal').textContent=events.length;$('#activity').innerHTML=events.length?events.slice(0,9).map(x=>`<div><time>${x.created_at?new Date(x.created_at).toLocaleTimeString([],{hour:'numeric',minute:'2-digit'}):''}</time><span>${escapeHtml(x.message||'Verified activity')}</span><b>VERIFIED</b></div>`).join(''):'<div><time>NOW</time><span>No verified execution events yet.</span><b>IDLE</b></div>';$('#progress').innerHTML=[['Opportunities Found',p.found||0],['Analyses Completed',p.qualified||0],['Proposals Drafted',p.proposals||0],['Outreach Emails',p.outreach||0],['Approvals Pending',decisions.length]].map(x=>`<li>${x[0]} <b>${x[1]}</b></li>`).join('');$('#viewAgents').onclick=showAllAgents}function wireImage(){const img=$('#officeReference'),err=$('#imageError');if(!img)return;img.onerror=()=>{err.hidden=false};img.onload=()=>{err.hidden=true};if(img.complete&&!img.naturalWidth)err.hidden=false}function escapeHtml(s=''){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function showAllAgents(){const working=registry.agents.filter(a=>state(a.id)==='working'),waiting=registry.agents.filter(a=>state(a.id)==='awaiting_approval'),idle=registry.agents.filter(a=>state(a.id)==='live_idle');$('#detailBody').innerHTML=`<small>PROCESSPILOT AI WORKFORCE</small><h2>${registry.agents.length} Live AI Employees</h2><p>${working.length} working now · ${waiting.length} awaiting approval · ${idle.length} waiting for work</p><div class="detail-section"><h3>ALL AGENTS</h3>${registry.agents.map(a=>`<button class="badge" data-agent="${a.id}">${escapeHtml(a.name)} · ${stateLabel(a.id)}</button>`).join('')}</div>`;$('#detail').showModal();document.querySelectorAll('[data-agent]').forEach(b=>b.onclick=()=>showAgent(b.dataset.agent))}function showAgent(id){const a=registry.agents.find(x=>x.id===id);if(!a)return;$('#detailBody').innerHTML=`<small>AI EMPLOYEE // ${escapeHtml(a.department)}</small><h2>${escapeHtml(a.name)}</h2><p><b>${stateLabel(id)}</b> · ${escapeHtml(a.mission)}</p><div class="detail-section"><h3>INPUTS</h3><p>${a.inputs.map(escapeHtml).join(' · ')}</p></div><div class="detail-section"><h3>OUTPUTS</h3><p>${a.outputs.map(escapeHtml).join(' · ')}</p></div><div class="detail-section"><h3>TOOLS</h3><p>${a.tools.map(escapeHtml).join(' · ')}</p></div><div class="detail-section"><h3>GOVERNANCE</h3><p>${escapeHtml(a.approval)}</p></div><div class="detail-section"><h3>NEXT HANDOFF</h3><p>${escapeHtml(a.handoff)}</p></div>`;$('#detail').showModal()}$('#closeDetail').onclick=()=>$('#detail').close();function clock(){const d=new Date();$('#clock').textContent=d.toLocaleTimeString('en-US',{timeZone:'America/Chicago',hour:'numeric',minute:'2-digit',second:'2-digit'});$('#dateLabel').textContent=d.toLocaleDateString('en-US',{timeZone:'America/Chicago',weekday:'short',month:'short',day:'numeric',year:'numeric'})}setInterval(clock,1000);clock();load();setInterval(load,60000);